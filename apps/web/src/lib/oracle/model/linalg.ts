/**
 * A2 — Linear algebra helpers for the logistic/multinomial fitter (STUB, Phase A).
 * Vector/matrix primitives (dot, matvec, transpose, solve) used by fitter.ts.
 * Pure, dependency-free, numerically guarded.
 */

export function dot(_a: number[], _b: number[]): number {
  throw new Error('TODO: A2 — linalg.ts');
}
